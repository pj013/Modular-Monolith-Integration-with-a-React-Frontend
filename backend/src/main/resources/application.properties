spring.application.name=edu

spring.datasource.url=jdbc:postgresql://aws-0-ap-southeast-2.pooler.supabase.com:5432/postgres?sslmode=require
spring.datasource.username=postgres.pphibwmmzaljfdibkpyw
spring.datasource.password=EtTlz4jhsRAgWURb
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

app.cors.allowed-origin=http://localhost:5173

# Stock level below which a LowStockEvent is published after a successful reserve()
app.inventory.low-stock-threshold=${LOW_STOCK_THRESHOLD:5}

# Serialize LocalDateTime as an ISO-8601 string ("2026-09-10T20:15:00") instead of
# Jackson's default array form ([2026,9,10,20,15,0]) - much easier to consume in React.
spring.jackson.serialization.write-dates-as-timestamps=false